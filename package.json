{
  "name": "agent-badge",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "cd packages/saas && bun run build",
    "start": "cd packages/saas && bun run start"
  }
}
